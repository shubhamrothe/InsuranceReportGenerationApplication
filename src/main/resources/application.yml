server:
  port: 8010

spring:
  application:
    name: Insurance_Report_App

  datasource:
    url: jdbc:mysql://localhost:3306/insurance_reports
    username: root
    password: Shubham0342@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

 
  mvc:
    view:
      prefix: /pages/
      suffix: .jsp